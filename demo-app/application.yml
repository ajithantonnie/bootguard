server:
  port: 8080

spring:
  profiles:
    active: prod
  datasource:
    url: jdbc:mysql://localhost:3306/db?useSSL=false
    username: root
    password: admin123

management:
  endpoints:
    web:
      exposure:
        include: "*"

debug: true
api-key: "sk-1234567890"

custom-config:
  stripe-sk: "STRIPE_SK_PLACEHOLDER"
  github-pat: "GITHUB_PAT_PLACEHOLDER"
  
app:
  name: "my-spring-boot-application"
